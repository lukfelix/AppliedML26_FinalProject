*.pyc
.vscode/

# put any output files that shouldn't be uploaded in a separate directory
outputs/*

__pycache__/

# to avoid getting environments in accidently
myenv/

# ignore any WSL zone identifiers
*Zone.Identifier
.DS_Store

# not storing weights of saved models
*.pt

# the lightcurve data is too large to be uploaded, so we ignore it
data/lightcurves.h5
